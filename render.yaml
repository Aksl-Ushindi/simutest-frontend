services:
  - type: web
    name: simutest-frontend
    env: node
    region: frankfurt
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run start
    rootDir: .
    autoDeploy: true
